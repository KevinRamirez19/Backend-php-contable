{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "rm -rf vendor composer.lock && composer clear-cache && COMPOSER_NO_CACHE=1 composer install --no-dev --optimize-autoloader --no-scripts --prefer-dist && php artisan config:cache && php artisan route:cache && php -S 0.0.0.0:$PORT public/index.php",
    "restartPolicyType": "ON_FAILURE"
  }
}