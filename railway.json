{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "composer dump-autoload && php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear && php artisan config:cache && php artisan route:cache && echo ✅ Caches cleared && php -S 0.0.0.0:$PORT -t public",
    "restartPolicyType": "ON_FAILURE"
  }
}