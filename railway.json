{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && while true; do php -S 0.0.0.0:$PORT -t public; sleep 1; done",
    "restartPolicyType": "ON_FAILURE"
  }
}