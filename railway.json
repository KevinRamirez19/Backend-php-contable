{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "rm -rf vendor composer.lock && composer clear-cache && composer install --no-dev --optimize-autoloader --no-scripts && php artisan config:cache && php artisan route:cache && sleep 10 && php artisan migrate --force",
    "restartPolicyType": "ON_FAILURE"
  }
}